const Vendor = require('./vendor.models');
const Menu = require('./menu.models');
const VendorCategory = require('./vendor-category.models');
const Combo = require('./combo.models');
const Search = require('./search.models');
const FavouriteMeal = require('./favourite-meal.models');
const User = require('./user.models');
const Cart = require('./cart.models');
const Card = require('./card.models');
const Transaction = require('./transaction.models');
const Order = require('./order.models');
const Address = require('./address.models');
const Wallet = require('./wallet.models');

module.exports = {
  Vendor,
  Menu,
  VendorCategory,
  Combo,
  Search,
  FavouriteMeal,
  User,
  Cart,
  Card,
  Transaction,
  Order,
  Address,
  Wallet,
};
